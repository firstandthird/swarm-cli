#!/bin/bash

main() {
  local cmd=$@
  if test -z "$cmd"; then
    log "SSH'ing into $SERVER_HOST"
  else
    log "Running $cmd on $SERVER_HOST"
  fi
  ssh -t \
    ${SSH_KEY:+ -i ${SSH_KEY}} \
    $SSH_USER@$SERVER_HOST \
    $cmd
}
main "$@"
