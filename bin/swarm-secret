#!/bin/bash

main() {
  local configFile=$1
  local name=$2
  if test -z "$name"; then
    name=$configFile
  fi
  . $DIR/swarm-copy $configFile
  . $DIR/swarm-ssh "docker secret create $name $configFile"
}
main "$@"
